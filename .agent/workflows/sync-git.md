---
description: Sync changes to remote repository automatically
---

// turbo-all

1. Check current git status
```
git status
```

2. Add all changes
```
git add .
```

3. Commit changes with a descriptive message
```
git commit -m "chore: comprehensive bug audit and fixes for financial and security issues"
```

4. Push to remote
```
git push
```
