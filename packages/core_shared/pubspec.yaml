name: core_shared
description: Core shared models and utilities for WawApp
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^5.4.4
  firebase_messaging: ^15.2.10
  firebase_auth: ^5.3.0
  firebase_dynamic_links: ^6.1.10
  firebase_analytics: ^11.3.3
  firebase_crashlytics: ^4.1.3
  go_router: ^12.1.3
  intl: ^0.20.2
  connectivity_plus: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true