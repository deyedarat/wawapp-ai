name: auth_shared
description: Shared authentication logic for WawApp
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.4.4
  cloud_functions: ^5.1.3
  crypto: ^3.0.3
  flutter_riverpod: ^2.4.9
